////// DUNG DE TEST //////////////////////////////////////////////////////////////




/*const initialState = () => ({
    currentRecipe: null,
})

export const state = initialState()
export const getters = { //view lay gia tri currentRecipe: null,
    ...
}
----------------------------------------------------------------
export const actions = {
    ...
    // fetchData()... lay data tu server, khi trong view ta goi no de gan du lieu cho currentRecipe.
}

const moduleUpdate = {
    namespaced: true,
    state: () => ({
      dataRecipe: null
    }),
    mutations:{
      ADD(state,x)
      {
        state.dataRecipe = { id:0, tieude: "mon 0", cachlam: 'cach lam mon 0 o day', nguyenlieu: "nguyenlieu0"  };
        console.log(state.dataRecipe);
      }

  },
    actions: {
      fetchData(context,x) {
        console.log("da goi add module voi id = "+x);
        context.commit('ADD',x);
      }
    },
  
    getters: {
      getdata: state => {
        console.log("da goi getdata module");
        return state.dataRecipe;
      }
    }
  }
export default moduleUpdate
  */